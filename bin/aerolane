#!/usr/bin/env node

'use strict';

var program = require('commander');
program
  .version('0.0.1')
  .command('init', 'initialize aerolane')
  .command('build', 'build artifacts')
  .command('serve', 'start server')
  .command('publish', 'publish to device')
  .parse(process.argv);
