#!/usr/bin/env node

'use strict';

var program = require('commander');
program
  .version('0.0.1')
  .command('gradle', 'build with gradle')
  .command('carthage', 'build with carthage')
  .parse(process.argv);
